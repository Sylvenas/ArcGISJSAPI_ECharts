var dojoConfig = {
    parseOnLoad: true,
    packages: [{
        "name": "src",
        "location": "/src"//for demo;本地引用地址
        //"location": location.pathname.replace(/\/[^/]+$/, '') + "/src"    网络引用地址
    }]
};